# .github

Organization-level defaults for `tokeira`. `profile/README.md` renders on
the organization page once this repository is public — flipping it public
is a flip-day action (see `tokeira/ops` → `runbooks/flip-day.md`).
